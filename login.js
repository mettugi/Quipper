describe('Login', () => {
  it('Login Valid"', () => {
    cy.visit('https://www.saucedemo.com/')

    //cy.get('.user-name')
      //.type(standard_user)
      //.get('password')
      //.type('secret_sauce')
      //.click('login-button)
  })
})
